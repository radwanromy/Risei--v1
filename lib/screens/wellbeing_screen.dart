import 'package:flutter/material.dart';

class WellbeingScreen extends StatelessWidget {
  const WellbeingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Well-being Screen'),
    );
  }
}
